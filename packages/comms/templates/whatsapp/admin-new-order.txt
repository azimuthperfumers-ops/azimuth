Template name: azimuth_admin_new_order
Language: English
Category: Utility

Header (Text):
Azimuth Admin

Body:
A new order {{order_number}} has been placed for {{amount}}. Payment is confirmed and shipment booking is underway. Open the admin panel to review and process the order.

Button (Call to Action):
Type of Action: Visit website
Button Text:    View order
URL (dynamic):  https://admin.azimuth.net.in/orders/{{1}}

Variables:
{{order_number}} = order number (body)
{{amount}}       = amount, e.g. ₹1,234 (body)
{{1}}            = button URL suffix — code sends the order UUID

Note: Meta rejects short bodies with many variables ("too many variables for its
length") — keep body sentences long enough relative to variable count.
